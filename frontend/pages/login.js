import {useState} from "react";import {api} from "../src/lib/api";export default function Login(){const[email,setEmail]=useState("admin@agentur.local");const[password,setPassword]=useState("admin123");const[error,setError]=useState("");async function submit(e){e.preventDefault();try{const data=await api("/api/auth/login",{method:"POST",body:JSON.stringify({email,password})});localStorage.setItem("mmos_token",data.token);location.href="/agency"}catch(err){setError(err.message)}}return <main className="review-public"><form className="review-box" onSubmit={submit} style={{maxWidth:440}}><div className="brand" style={{justifyContent:"center"}}><div className="brand-mark">M</div><div className="brand-title" style={{color:"#111827"}}>Mecklenburg<br/>Marketing OS</div></div><p className="muted">Admin 1: admin@agentur.local / admin123<br/>Admin 2: mitarbeiter@agentur.local / mitarbeiter123</p><input className="input" value={email} onChange={e=>setEmail(e.target.value)} /><input className="input" type="password" value={password} onChange={e=>setPassword(e.target.value)} />{error&&<p style={{color:"#ef4444"}}>{error}</p>}<button className="btn">Einloggen</button></form></main>}