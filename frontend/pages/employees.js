import {useEffect,useState} from "react";import {api} from "../src/lib/api";import AppShell from "../src/components/AppShell";export default function Employees(){const[employees,setEmployees]=useState([]);const[form,setForm]=useState({name:"",email:"",role:"staff"});async function load(){setEmployees(await api("/api/employees"))}useEffect(()=>{load().catch(()=>location.href="/login")},[]);async function add(){await api("/api/employees",{method:"POST",body:JSON.stringify(form)});load()}return <AppShell active="employees"><div className="page-head"><div><h1>Mitarbeiter App</h1><p>Admin- und Teamzugänge verwalten</p></div><button className="btn" onClick={add}>＋ Mitarbeiter</button></div><div className="card"><div className="grid-3"><input className="input" placeholder="Name" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/><input className="input" placeholder="E-Mail" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/><select className="input" value={form.role} onChange={e=>setForm({...form,role:e.target.value})}><option>agency_admin</option><option>staff</option><option>readonly</option></select></div></div><div className="card"><table className="table"><thead><tr><th>Name</th><th>E-Mail</th><th>Rolle</th><th>Status</th></tr></thead><tbody>{employees.map(e=><tr key={e.id}><td>{e.name}</td><td>{e.email}</td><td>{e.role}</td><td><span className="badge">Aktiv</span></td></tr>)}</tbody></table></div></AppShell>}