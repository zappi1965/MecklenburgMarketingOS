import {useState} from "react";import {api} from "../src/lib/api";import AppShell from "../src/components/AppShell";export default function LeadScraper(){const[query,setQuery]=useState("Friseure Rostock");const[results,setResults]=useState([]);async function run(){const data=await api("/api/scraper/search",{method:"POST",body:JSON.stringify({query})});setResults(data)}return <AppShell active="lead-scraper"><div className="page-head"><div><h1>Lead Scraper</h1><p>Admin Tool für lokale Akquise-Listen</p></div><button className="btn" onClick={run}>🔎 Suche starten</button></div><div className="card"><input className="input" value={query} onChange={e=>setQuery(e.target.value)} /></div><div className="card"><table className="table"><thead><tr><th>Firma</th><th>Branche</th><th>Telefon</th><th>E-Mail</th><th>Bewertung</th><th>Aktion</th></tr></thead><tbody>{results.map(r=><tr key={r.id}><td>{r.company}</td><td>{r.category}</td><td>{r.phone}</td><td>{r.email}</td><td>{r.rating}</td><td><button className="btn secondary">Ins CRM</button></td></tr>)}</tbody></table></div><p className="todo">Hinweis: Echte Google-Maps-Daten bitte nur über offizielle APIs oder rechtssichere Datenquellen ziehen.</p></AppShell>}