
import { useState } from "react";
export default function Review(){const[r,setR]=useState(0);const[f,setF]=useState("");function send(){if(r<=3){location.href=`mailto:feedback@mecklenburg-marketing.de?subject=Internes Feedback&body=${encodeURIComponent("Bewertung: "+r+" Sterne\n\n"+f)}`}else{location.href="https://google.com/search?q=friseur+profi+bewertung"}}return <main className="page"><div className="box"><div className="brand"><div className="logo">★</div><div><b>Friseur Profi</b><br/><span className="muted">Bewertung abgeben</span></div></div><h1>Wie zufrieden warst du?</h1><p className="muted">1–3 Sterne gehen intern per Mail, 4–5 Sterne öffnen Google.</p><div className="stars">{[1,2,3,4,5].map(n=><span key={n} onClick={()=>setR(n)}>{n<=r?"★":"☆"}</span>)}</div>{r>0&&r<4&&<textarea className="input" rows="5" placeholder="Was können wir verbessern?" value={f} onChange={e=>setF(e.target.value)}/>} {r>0&&<button className="btn" onClick={send}>Bewertung absenden</button>}<a href="/"><button className="btn secondary">Zur Admin-Demo</button></a></div></main>}
